/** How many layers or groups should used for a test run. */
export const layerCount = 200;
/** The number of objects that will be drawn on each layer or group. */
export const objectsPerLayer = 50;
/** How often the benchmark will be repeated before results are shown. */
export const repitions = 1000;
/** If this value is false, Konva groups will be used instead of layers. */
export const useLayers = false;
